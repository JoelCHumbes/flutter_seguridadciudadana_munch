
import 'package:flutter/material.dart';

class FormCorreo extends StatelessWidget {
  const FormCorreo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          "Datos Correo",
        ),
      ),
    );
  }
}
